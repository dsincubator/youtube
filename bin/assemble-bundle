#!/usr/bin/env bash
set -u
DIR="$(cd "$(dirname "$0")" && pwd)"
echo "[assemble-bundle] deprecated: 'bundle' → 'wiki' — forwarding to bin/assemble-wiki (use --wiki-name/--wiki-tag, --from/--to)" >&2
exec "$DIR/assemble-wiki" "$@"
