---
okf_version: '0.2'
type: DirectoryIndex
title: Manifests Index
description: Index of fetch manifests for youtube.
tags:
- ds-incubator
- manifests-index
---

# Manifests Index (Youtube)

Fetch manifests kept readable after raw clean (not in `assets/*.tar.gz`).

- [metadata.tsv](metadata.tsv) — `metadata` fetch manifest (`id | status | reason | file`)
- [transcripts.tsv](transcripts.tsv) — `transcripts` fetch manifest (`id | status | file | lang`)
